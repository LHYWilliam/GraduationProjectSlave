{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "cube-cmake --build C:/Code/Embedded/C/GraduationProjectSlave/build/Debug --"
        },
        {
            "label": "Flash",
            "type": "shell",
            "command": "probe-rs run --chip STM32F103CB ./build/Debug/GraduationProjectSlave.elf --preverify --verify"
        },
        {
            "label": "Build + Flash",
            "type": "shell",
            "dependsOn": [
                "Build",
                "Flash"
            ]
        }
    ]
}